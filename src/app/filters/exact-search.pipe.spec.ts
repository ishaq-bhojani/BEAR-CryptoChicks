import { ExactSearchPipe } from './exact-search.pipe';

describe('ExactSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ExactSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
